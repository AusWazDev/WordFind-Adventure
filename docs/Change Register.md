# SoundFind — Change Register

**Project:** SoundFind
**Repo:** github.com/AusWazDev/WordFind-Adventure
**Baseline commit:** `129f64f` — *CR-07 to CR-10: dead code cleanup, offline fixes, How to Play update, 12 free hints*
**Baseline date:** 27 March 2026

All future changes must be raised as a Change Request, approved before implementation, and recorded here upon completion.

---

## Change Request Log

| CR # | Title | Status | Commit | Date |
|------|-------|--------|--------|------|
| CR-01 | Remove Spelling Bee mode entirely | ✅ Completed | `a80a6ae` | Mar 2026 |
| CR-02 | Promote Mystery Word to secondary hero card; reorder remaining tiles | ✅ Completed | `a80a6ae` | Mar 2026 |
| CR-03 | Add Lightbulb hint button to all non-audio word list modes (−25% penalty) | ✅ Completed | `a80a6ae` | Mar 2026 |
| CR-04 | Expand Word Association clues from ~50 to ~800 entries (all categories + tricky audio words) | ✅ Completed | `a80a6ae` | Mar 2026 |
| CR-05 | Restrict Mystery Word filler pool to active category only | ✅ Completed | `a80a6ae` | Mar 2026 |
| CR-06 | Full-grid Mystery Word coverage — all remaining empty cells form the mystery word | ✅ Completed | `a80a6ae` | Mar 2026 |
| CR-07 | Remove dead `spelling: 'Spelling Bee'` entry from `GameHeader.jsx` modeLabels | ✅ Completed | `129f64f` | Mar 2026 |
| CR-08 | Remove stale `REQUIRES_ONLINE` gate for Word Association (works fully offline after CR-04) | ✅ Completed | `129f64f` | Mar 2026 |
| CR-09 | Update `HowToPlayModal` — Mystery Word added, Spelling Bee removed, clue description fixed, hint penalties documented | ✅ Completed | `129f64f` | Mar 2026 |
| CR-10 | Offline monetisation — 12 free hints on first launch; interstitial ad skipped offline; HintModal buttons greyed out offline | ✅ Completed | `129f64f` | Mar 2026 |
| CR-11 | Remove dead code in `gameUtils.jsx` — `pickCategoryBonusWord`, `tryPlaceWordDenseProtected`, `hasEmptyNonProtectedCell` | ✅ Completed | `3aba629` | Mar 2026 |
| CR-12 | Settings page refresh — add Master level, all 23 categories, back button, auto-save indicator, remove Theme stub, rename Delete Account → Reset Game Data | ✅ Completed | `3aba629` | Mar 2026 |
| CR-13 | Daily Challenge overhaul — fixed viewport layout matching Game.jsx, orientation support, board sizing, remove SpellingBeeWordList, add mystery_word support, fix hintCell→hintCells, fix hints hardcoded to 3; replace 2 spelling templates in DailyChallengeUtils; fix MODE_LABELS in DailyChallengeCard | ✅ Completed | `7470c5c` | Mar 2026 |
| CR-14 | Delete orphaned `SpellingBeeWordList.jsx` — no longer imported anywhere after CR-13 | ✅ Completed | `3561220` | Mar 2026 |
| CR-15 | Reduce interstitial ad frequency — change trigger from every 3 game **starts** to every 6 **completed** games. `AD_FREQUENCY` changed to 6 in `Home.jsx`; `saveProgress` in `Game.jsx` now increments `games_completed_count` in localStorage; `handleSelectLevel` reads `games_completed_count` and `last_ad_completed_at` to determine when to show the ad, avoiding double-triggering at the same threshold. | ✅ Completed | `caa3330` | Mar 2026 |
| CR-16 | Collapsible word list — word list panel is **collapsed by default** during play, showing a slim pill bar ("Words to Find · 3/8 ▾"). Player taps the bar to expand the full list; taps again to re-collapse. The progress pill turns primary-colour when all words are found. Auto-expands when the bonus word hunt begins (so all ticked words are visible) and on non-bonus victory. Landscape sidebar and all three word list variants (Standard/Audio, Anagram, Association) are unaffected — collapse wrapper lives entirely in `Game.jsx`'s portrait layout, keeping rollback trivial. | ✅ Completed | `6a45e9e` | Apr 2026 |
| CR-17 | Responsive grid sizing — board `maxHeight` in portrait mode increases from `min(55dvh, 100vw)` to `min(75dvh, 100vw)` when the word list is collapsed (CR-16 default state). Board measurement effect re-fires on collapse toggle, so the grid recalculates to fill the extra vertical space. When the word list is expanded, board reverts to `55dvh` cap and word list panel takes `flex: 1`. Landscape layout unchanged. | ✅ Completed | `6a45e9e` | Apr 2026 |
| CR-18 | App icon design — interactive HTML5 Canvas mockup (`docs/icon/icon-mockup.html`) and final 1024px master PNG (`docs/icon/soundfind-icon-d.png`). Concept D: diagonal split with a 7×7 word grid in the top-right triangle (fading toward the diagonal), SOUND and FIND spelled in teal-highlighted cells along the two anti-diagonals, and a rounded iOS-style teal speaker in the top-left corner with gold accent waves radiating toward the grid. Final locked settings: grid opacity 38%, grid fade 60%, wave reach 72%, wave start gap 10%, speaker size 20%, speaker length 150%, bottom bleed 40%, top bleed 47%, speaker position x=15% y=15%. | ✅ Completed | `83db442` | Apr 2026 |
| CR-19 | Splash screen design — interactive HTML5 Canvas mockup (`docs/icon/splash-mockup.html`) for the app launch splash screen. Shows the CR-18 icon centred on a deep dark background with "SoundFind" in white/violet below and tagline "Find the words. Feel the sound." Fade in (400ms) → hold → fade to Home (300ms) animation sequence with Preview button. Final locked settings: icon size 40%, icon vertical position 40%, name spacing 10%, tagline "Find the words. Feel the sound.", background deep dark near-black, grid overlay opacity 10%, hold duration 2000ms (total 2.7s). | ✅ Completed | `48501a2` | Apr 2026 |
| CR-21 | Brand alignment — WelcomeScreen and HowToPlayModal updated to match the dark splash/icon design language. WelcomeScreen background changed from purple gradient to deep dark near-black (`#0f0e1a → #1a1830`). Volume2 placeholder icon replaced with `public/icon.png` (72px, iOS-style border-radius). "Find" text colour changed from `amber-400` to `violet-400`. Audio Challenge feature card changed from amber/orange gradient to teal/indigo to match the icon's speaker colour. Top-left decorative blob changed from amber to violet. HowToPlayModal featured-slide header (Audio Challenge) changed from amber/orange to teal/indigo gradient — all slides now use the same violet/indigo family. `Volume2` unused import removed from `WelcomeScreen.jsx`. | ✅ Completed | `dc87dcd` | Apr 2026 |
| CR-28 | WelcomeScreen redesign (Option B — minimal + one-liner). Removed Audio Challenge hero card and 4-tile feature grid. Icon enlarged to 88px. One-liner pill added: "5 game modes · Natural voices · Works offline" (no border). Primary button changed to violet→indigo gradient matching app hero cards. Secondary "How to Play" button changed to teal outline — clearly distinct. Bottom blob changed to teal. Removed misleading "Your progress is saved automatically" footer (progress only saves on game completion, not mid-game). | ✅ Completed | `aefc0d1` | Apr 2026 |
| DEF-28 visual | Hint buttons now visually disabled while a hint is active. `GameHeader.jsx`: `hintActive` prop added; both hint buttons dim to 40% opacity with `cursor-not-allowed` and no hover/tap animation when locked. `WordList.jsx`: lightbulb buttons (audio + non-audio paths) dim to 40% when `hintWord` is set. `Game.jsx`: passes `hintActive={!!hintWord}` to `headerProps`. Previously the lockout logic was correct but buttons showed no visual feedback. | ✅ Fixed | `aefc0d1` | Apr 2026 |
| DEF-29 | All duplicate keys in `wordClues` object resolved — 38 total fixed across two commits. Words appearing in multiple categories (food+colour, nature+tech, ocean+animals, etc.) now have a single combined clue covering both meanings. Silent-letter notes merged into primary clues for CASTLE, TOMB, LAMB, SALMON, CALM, ISLAND, KNIGHT, SWORD, ARMOUR. Near-identical duplicate clues (FOSSIL, PENGUIN, REEF, LOBSTER, AURORA, CRYSTAL, GRAVITY, LAGOON, OYSTER, CLUSTER, MOLE, and others) collapsed to one canonical entry. CACHE clue added (was in tech word list but missing from wordClues). COOKIE clue updated to combined food+tech meaning. Root cause: flat `wordClues` object means words in multiple categories can only have one clue — last entry wins silently. | ✅ Fixed | `38416fe` + `aefc0d1` | Apr 2026 |
| DEF-28 | Hint flash now persistent until hinted word is found. Previously: 4-second timer cleared the flash; rapid taps consumed multiple hints with no visual feedback. Fix: timer removed from regular hint path in `handleUseHint` and `handleHintCell`; `hintWord` guard added to both functions to block re-entry while a hint is active; `hintWordRef` added and synced via `useEffect`; `handleWordFound` clears `hintCells`/`hintWord` when the matching word is found. Bonus-hunt hint retains 4s timer (separate path). | ✅ Fixed | `38416fe` | Apr 2026 |
| CR-27 | Word list expanded by default — reverts CR-16's `useState(true)` to `useState(false)` in `Game.jsx`. Collapsed-by-default wasn't discoverable on mobile; players didn't realise their word list was hidden. Player can still collapse manually via the toggle bar. FE-02 logged in `docs/Future Enhancements.md` for a smarter collapsed-default UX in a future version. | ✅ Completed | `d9540f5` | Apr 2026 |
| DEF-30 | Word list collapsed on game start despite CR-27 expanded-by-default. Root cause: CR-27 correctly set `useState(false)` but the `generateGame` effect still called `setWordListCollapsed(true)` on every new game, overriding the default. Fixed by changing that call to `false` in `Game.jsx`. | ✅ Fixed | `5d67343` | Apr 2026 |
| CR-30 | Audit fixes — unused imports removed (Layout, AssociationWordList, DailyChallengeCard, ReminderSettings), dead file `src/lib/app-params.js` deleted (Base44 leftover), `alert()` IAP stubs replaced with `toast.info()` in HintModal and RemoveAdsModal, iOS PWA meta tags added to index.html (`apple-mobile-web-app-capable`, `apple-mobile-web-app-status-bar-style`, `apple-mobile-web-app-title`), Traceability.md date updated. No functional behaviour changes. | ✅ Completed | `6d674a7` | Apr 2026 |
| CR-29 | HowToPlay modal redesign — responsive sizing and declutter. Modal width now scales with viewport (`max-w-sm` → `sm:max-w-md` → `md:max-w-lg`). Modal height uses `flex flex-col max-h-[90dvh]` so it shrinks to content and only scrolls when genuinely needed. Scrollbar hidden via `.no-scrollbar` CSS utility (scroll still works via touch/wheel). Header gradient fixed to violet→indigo on all slides (previously switched to amber/orange on Audio Challenge slide). Nav button gradient fixed to violet→indigo on all slides. All body text standardised to `text-sm`. Slide content trimmed by ~35% — removed amber highlight box, reduced Audio Challenge examples from 5 to 3, replaced coloured mode cards with clean divider list, removed scoring grid from Hints slide. Game mode list order now matches the home screen menu (Audio Challenge → Mystery Word → Standard → Word Association → Anagram Hunt). `no-scrollbar` utility added to `src/index.css`. Unused `Trophy` import removed. | ✅ Completed | `79f75d8` | Apr 2026 | Modal width now scales with viewport (`max-w-sm` → `sm:max-w-md` → `md:max-w-lg`). Modal height uses `flex flex-col max-h-[90dvh]` so it shrinks to content and only scrolls when genuinely needed. Scrollbar hidden via `.no-scrollbar` CSS utility (scroll still works via touch/wheel). Header gradient fixed to violet→indigo on all slides (previously switched to amber/orange on Audio Challenge slide). Nav button gradient fixed to violet→indigo on all slides. All body text standardised to `text-sm`. Slide content trimmed by ~35% — removed amber highlight box, reduced Audio Challenge examples from 5 to 3, replaced coloured mode cards with clean divider list, removed scoring grid from Hints slide. Game mode list order now matches the home screen menu (Audio Challenge → Mystery Word → Standard → Word Association → Anagram Hunt). `no-scrollbar` utility added to `src/index.css`. Unused `Trophy` import removed. | ✅ Completed | TBD | Apr 2026 |
| CR-26 | Mode reorder + Mystery Word difficulty cap — Word Association moved above Anagram Hunt in `GameModeSelector.jsx`. `LevelSelector.jsx` now accepts `mode` and `category` props; when Mystery Word + a specific (non-Random) category is selected, Expert and Master are hidden entirely — only Easy, Medium, Hard are shown (tracked as FE-01 for future fix). `Home.jsx` passes `selectedMode` + `selectedCategory` to `LevelSelector`. DEF-27 follow-up: removed `emptyAfter > 0` guard in filler undo so the undo also fires when a word fills all remaining empty cells. Duplicate `SALMON` key in `wordClues` removed (colours entry deleted; food entry retained). `docs/Future Enhancements.md` created with FE-01. | ✅ Completed | `a18a09f` | Apr 2026 |
| CR-25 | Code audit cleanup — 9 issues fixed from full codebase audit: (1) dead imports removed (`speakText` from WordList/Game, `useMemo` named import from Stats, `Toaster` from Game/DailyChallenge); (2) duplicate `<Toaster />` providers consolidated — App.jsx now mounts the single global sonner Toaster, Game.jsx and DailyChallenge.jsx no longer mount their own; (3) `WordList.jsx` direct `localStorage` read replaced with `getLocalSettings()` ensuring DEFAULT_SETTINGS merge; (4) `LEVEL_NAMES`, `CATEGORIES`, `CATEGORY_LABELS` extracted to `src/lib/constants.js` — eliminates duplicate declarations across GameHeader, VictoryModal, Settings; (5) `best_streak` bug fixed — DailyChallenge's `triggerVictory` now computes `newStreak` and includes `best_streak` in the `updateProgress` call; (6) hint timer leak fixed — `hintTimerRef` added to Game.jsx so each hint cancels the previous 4s clear-timer; (7) DailyChallenge score penalty aligned with Game.jsx — lightbulb hints now apply 0.75× penalty (requires `hintedWords` state + ref added); (8) stale "AUTO-GENERATED" comment in `pages.config.js` replaced with accurate description; (9) 45 unused shadcn UI files deleted from `src/components/ui/` — CSS bundle reduced from 97 kB → 58 kB. | ✅ Completed | `a584696` | Apr 2026 |
| CR-24 | Word Association clue update — ~450 new `wordClues` entries added to `gameUtils.jsx` covering all words added in DEF-20 and DEF-24 expansions across all 13 categories (animals, colours, food, nature, music, countries, science, mythology, technology, ocean, history, emotions, sports, space). Clues replace the previous `A ${n}-letter word` fallback for every expanded word. Format matches existing entries: short, descriptive phrases. | ✅ Completed | `1d6f97d` | Apr 2026 |
| CR-22 | Pre-generated audio via ElevenLabs — replaces Web Speech API for all in-game audio with natural Australian voices (Hannah female / Neil male, `eleven_turbo_v2_5`). Three audio libraries generated: `public/audio/{female\|male}/{WORD}.mp3` (1,714 words × 2 genders), `public/audio/phrases/{gender}_{key}.mp3` (3 fixed phrases × 2 genders), `public/audio/sentences/{gender}_{WORD}.mp3` (313 tricky-word sentences × 2 genders — each a seamless single recording of "word... sentence... word."). `scripts/generate-audio.mjs` generates all three phases; resumable (skips existing files). `voiceUtils.jsx` replaced HTML5 Audio with Web Audio API (`AudioContext`) for gapless playback — `playSeamless()` fetches files in parallel and schedules them sample-accurately back-to-back; `speakWordAudio()` plays word twice with intentional 400ms gap; `speakSentenceAudio()` plays sentence MP3 with Web Speech fallback; `speakPhraseAndWord()` and `speakFixedPhrase()` use gapless scheduling. `unlockAudio()` now also resumes `AudioContext` on first user gesture (iOS). `WordList.jsx`: sentence path uses `speakSentenceAudio()`; non-sentence path uses `speakWordAudio()`. `Game.jsx`: three `speakText` calls replaced with `speakPhraseAndWord` / `speakFixedPhrase`. Settings demo voice remains on Web Speech. | ✅ Completed | `922ac7e` + `7de9b53` | Apr 2026 |
| CR-34 | PageNotFound navigation — replaced `window.location.href = '/'` with `useNavigate()` from React Router in `PageNotFound.jsx`. Capacitor compatibility fix — hard reload replaced with client-side navigation for smooth transition back to Home. | ✅ Completed | `31985db` | Apr 2026 |
| CR-33 | PWA icon sizes + manifest fix — generated `icon-192.png` and `icon-512.png` from 1024px master. Updated `vite.config.js` manifest to include all three sizes with separate `any` and `maskable` purpose entries per PWA best practice. Required for Microsoft Store PWA submission via PWABuilder. | ✅ Completed | `a12686c` | Apr 2026 |
| CR-32 | Reduce interstitial ad frequency from 6 to 3 completed games — `AD_FREQUENCY` changed from 6 to 3 in `Home.jsx`. Rewarded ads and hint packs unchanged. Remove Ads at $2.99 (already stubbed, RevenueCat Phase 5). Interstitials to be configured as short-form (15s) in AdMob ad unit settings at Phase 5; rewarded ads remain 30s non-skippable. | ✅ Completed | `6cb4621` | Apr 2026 |
| CR-35 | Settings page — remove Game Preferences section, add About & Legal section. Game Preferences (default difficulty, preferred category) removed — redundant as players select both at game start. About & Legal card added at bottom of Settings: developer name (Unique Interactive Games), version (1.0.0), links to Privacy Policy (`uniquegames.com.au/soundfind/privacy/`), Support & Contact (`uniquegames.com.au/contact/`), and website. Required for app store submissions — Google Play mandates an in-app privacy policy link when ads are present. Unused imports `Target`, `CATEGORIES`, `DIFFICULTY_LEVELS` removed. | ✅ Completed | `b32691f` | Apr 2026 |
| CR-36 | Capacitor integration for native iOS and Android builds — installed `@capacitor/core`, `@capacitor/cli`, `@capacitor/ios`, `@capacitor/android`. Initialised with `appId: au.com.uniquegames.soundfind`, `webDir: dist`. Switched `BrowserRouter` → `HashRouter` in `App.jsx` (required for Capacitor WebView which cannot handle SPA HTML5 history routing). Added `ios/` and `android/` native project scaffolding. Updated `.gitignore` to exclude build artifacts. Updated `.gitattributes` — added mp3 binary, keystore binary, bat/cmd CRLF. Side effect: HashRouter moves URL params to the hash fragment — `window.location.search` becomes permanently empty (see DEF-31, DEF-36). | ✅ Completed | `43bc02f` | Apr 2026 |
| CR-37 | PWA manifest — added `id: '/'` and `orientation: 'any'` to the VitePWA manifest in `vite.config.js`. Required for PWABuilder to validate the manifest and generate a Windows MSIX package for Microsoft Store submission. | ✅ Completed | `efc913e` | Apr 2026 |
| CR-38 | Appearance setting — added Default / Light / Dark theme selector to `Settings.jsx`. Default follows the device's system preference (`prefers-color-scheme`); Light and Dark override it. `Layout.jsx` reads the saved `theme` field from `wf_settings` on mount and re-applies when the `soundfind-theme-changed` custom event fires (dispatched by `Settings.jsx` on change). Theme is applied immediately without reload. `DEFAULT_SETTINGS` already contained `theme: 'default'` so no migration needed. Replaces the DEF-40 forced-dark fix with a proper user-controlled preference. | ✅ Completed | `94850d6` | Apr 2026 |
| CR-31 | Hide "How to Play" button text on small screens — text label wrapped in `<span className="hidden sm:inline">` in `Home.jsx`. On narrow portrait phones only the HelpCircle icon shows; label reappears at sm breakpoint (640px+), which covers landscape mode on most phones. | ✅ Completed | `6754924` | Apr 2026 |
| CR-20 | Splash screen implementation — `src/components/game/SplashScreen.jsx` built from CR-19 approved design. Deep dark near-black gradient background, icon from `public/icon.png` at 40vmin with iOS-style border-radius, "Sound" (white) + "Find" (violet-400) name, tagline "Find the words. Feel the sound.", three violet loader dots. Framer Motion sequence: fade in 400ms → hold 2000ms → fade out 300ms → `onComplete` callback. Wired into `App.jsx` via `showSplash` state rendered above all routes. `public/icon.png` added (1024px master PNG from CR-18). `index.html` favicon updated from emoji to `icon.png`. | ✅ Completed | `4b4c408` | Apr 2026 |

---

## Defects Log

| DEF # | Description | Status | Fix Commit | Date |
|-------|-------------|--------|------------|------|
| CR-23 | PWA service worker for offline audio — installed `vite-plugin-pwa`; Workbox service worker precaches app shell (JS, CSS, HTML, icons) and applies a `CacheFirst` runtime caching strategy for all `/audio/**/*.mp3` requests (cache name `soundfind-audio-v1`, 1-year TTL, up to 5,000 entries). Any MP3 fetched while online is permanently cached on-device. Combined with `preloadGameAudio()` (DEF-26), opening one Audio Challenge game on WiFi caches all that game's audio — subsequent plays work fully offline with natural Hannah/Neil voices. PWA manifest added: name "SoundFind", standalone display, dark theme `#0f0e1a`, 1024px icon. App shell is also offline-capable so the app loads without a network connection. | ✅ Completed | `78858ab` | Apr 2026 |
| DEF-26 | Significant delay before audio played — browser was fetching and decoding MP3 from Vercel on every tap with no caching. Fix: added `_bufferCache` Map in `voiceUtils.jsx` so decoded `AudioBuffer` objects are reused across plays (repeat plays instant). Added `preloadGameAudio()` export that fires-and-forgets a background fetch of all word, sentence, and phrase MP3s for the current game immediately after `generateGame()` runs in audio mode. First taps are now instant as preload completes in the background during game setup. | ✅ Fixed | `ef2543f` | Apr 2026 |
| DEF-25 | Settings "Test Voice" button played robotic Web Speech API audio instead of ElevenLabs MP3s — `testVoice()` was still calling `speakText()` directly. Separately, when the last word was found, `speakPhraseAndWord('great_you_found')` and `speakFixedPhrase('all_words_found')` both fired simultaneously on the AudioContext, causing them to play over each other and the celebration to be inaudible. Also, non-bonus game completions had no celebration audio at all. Fix: `Settings.jsx` — replaced `speakText` import with `speakPhraseAndWord` + `unlockAudio`; test now plays "Great! You found rain!" via ElevenLabs. `Game.jsx` — consolidated last-word audio logic: when last word + bonus hunt, skip `great_you_found` entirely; when last word + no bonus, play new `game_complete` phrase instead. New phrase `game_complete` = "Incredible! You found all the words!" added to `generate-audio.mjs` and generated (2 new MP3s). | ✅ Fixed | `38cbbbf` | Apr 2026 |
| DEF-19 | Touch scroll conflicts with word selection on mobile web — dragging at an angle triggered browser scroll instead of word selection; downward drags triggered pull-to-refresh losing the game. Fix: added non-passive `touchstart` and `touchmove` listeners on the grid DOM node in `GameBoard.jsx` (React synthetic handlers are passive by default and cannot call `preventDefault`). Added `touch-action: none` and `overscroll-behavior: none` inline styles to the grid container. | ✅ Fixed | `caa3330` | Mar 2026 |
| DEF-01 | Word list displayed words in lowercase | ✅ Fixed | `a80a6ae` | Mar 2026 |
| DEF-02 | Words 100% formed from already-placed letters incorrectly included in word list | ✅ Fixed | `a80a6ae` | Mar 2026 |
| DEF-03 | "Homophone" label showing under RAIN in non-audio Mystery Word mode | ✅ Fixed | `ef74286` | Mar 2026 |
| DEF-04 | Category name truncated in compact header ("Mystery Word ·...") | ✅ Fixed | `ef74286` | Mar 2026 |
| DEF-05 | Non-category words appearing in themed Mystery Word filler (e.g. RUIN/SAD in Food game) | ✅ Fixed | `a80a6ae` | Mar 2026 |
| DEF-06 | HowToPlay nav buttons appeared permanently hovered/pressed | ✅ Fixed | `129f64f` | Mar 2026 |
| DEF-07 | HowToPlay Other Modes tile titles incorrectly inherited dark mode text colour | ✅ Fixed | `129f64f` | Mar 2026 |
| DEF-08 | Category and Level selector tile titles incorrectly inherited dark mode text colour (same class as DEF-07); icon and title now displayed inline | ✅ Fixed | `6931b72` | Mar 2026 |
| DEF-09 | WelcomeScreen feature grid references removed game mode "Spelling Bee" (🐝) — replaced with Mystery Word (🔍) | ✅ Fixed | `ce06d61` | Mar 2026 |
| DEF-10 | Audio Challenge word list dots (• • • • •) wrapping onto second line in narrow containers — added `whitespace-nowrap` to dot span in `WordList.jsx` | ✅ Fixed | `0be8117` | Mar 2026 |
| DEF-11 | Audio Challenge using robotic Microsoft voice instead of Google US English — `getVoices()` was caching only local (Microsoft) voices on first synchronous call before Chrome's `voiceschanged` fired with full list; removed early-return short-circuit in `voiceUtils.jsx` | ✅ Fixed | `b2ee77f` | Mar 2026 |
| DEF-12 | Audio Challenge voice quality poor on Safari/Mac — investigated on Mac (29 Mar). Safari returns 223 voices synchronously; no enhanced voices installed on test Mac. Scoring algorithm correctly selects Karen (en-AU, score 105). No code fix required — quality limited by macOS installed voices. iOS beta testers will have Karen (Enhanced) pre-installed and will get high-quality audio automatically. | ✅ Closed (no code change) | — | Mar 2026 |
| DEF-17 | Mystery word selected from wrong category — e.g. INTERSTELLAR (space) appearing in a Food game. Filler loop ran past all valid food word lengths, leaving K=11 which matched a space category bonus pair. Fix: pre-compute `validMysteryLengths` from the category word pool; filler loop now stops as soon as empty cell count hits a valid length, ensuring `findMysteryWord` always finds an in-category match. Cross-category fallback retained as true last resort. | ✅ Fixed | `80c20d0` | Mar 2026 |
| DEF-18 | In Mystery Word mode, words from the regular word list could have all their letters already present in other selected words (e.g. IRAN in a Countries game where I, R, A, N all appear elsewhere), making them trivially deducible. The unique-letter preference rule from DEF-16 was applied only to the mystery word selection, not to `pickWords`. Fix: added `preferUniqueLetters` flag to `pickWords`; in Mystery Word mode, words are greedily selected so each contributes at least one letter not already covered by the other selected words. Falls back to any valid word only if all 26 letters are already exhausted. Word pool remains the same themed category pool as Standard mode. | ✅ Fixed | `ea5f433` | Mar 2026 |
| DEF-16 | Mystery word could be selected with all letters already present in regular word list (e.g. PIE when P, I, E all used by other words) — `findMysteryWord` had no preference for words contributing unique letters. Fix: build `placedLetters` set from all placed words; pass to `findMysteryWord` which now prefers candidates containing at least one letter not in that set, falling back to any valid word only if no unique-letter candidate exists at the target length. | ✅ Fixed | `80c20d0` | Mar 2026 |
| DEF-15 | Level selector Medium tile appears permanently hovered — selected state used `shadow-lg shadow-violet-200` which is visually indistinguishable from the hover shadow; replaced with `ring-2 ring-violet-300 ring-offset-1` to give a clear selection indicator distinct from hover. | ✅ Fixed | `860b23a` | Mar 2026 |
| DEF-14 | Highlighting a substring of a longer word incorrectly accepted as finding the shorter word (e.g. highlighting CAKE within PANCAKE's grid positions marked CAKE as found). `checkWord` matched on spelling only — added position guard in `handleWordFound` to compare selected cells against `wordPositions[word]`; rejects match if cells don't align exactly. | ✅ Fixed | `ac1f345` | Mar 2026 |
| DEF-13 | App crashes when clicking Next Level after completing a level — `handleNextLevel` used `window.location.assign()` causing a hard reload to `/Game` which Vercel serves as 404 (no SPA routing configured). Fix: added `vercel.json` with SPA rewrites; replaced `window.location.assign()` with React Router `navigate()`; updated `useEffect` deps to `[level, mode, category]` so game re-initialises on client-side URL change. | ✅ Fixed | `2eb2b4e` | Mar 2026 |
| DEF-21 | Bonus word input auto-focuses on iOS, triggering the keyboard and scrolling/zooming the viewport to the input field — obscuring the grid the player needs to read the gold letters from. Two fixes in `Game.jsx`: (1) added `autoFocus={false}` to the input so the keyboard only appears when the player deliberately taps the field; (2) changed `fontSize` from 14 to 16px — iOS Safari auto-zooms any input with font-size < 16px, causing the viewport scale change. | ✅ Fixed | `660d991` | Apr 2026 |
| DEF-22 | Splash screen (CR-20) faded in from transparent, causing the Home screen to flash briefly before the splash covered it. Root cause: `SplashScreen.jsx` used `initial={{ opacity: 0 }}` on the whole overlay div, so for the 400ms fade-in the Home page was visible underneath. Fix: split into two motion elements — outer overlay starts at `opacity: 1` (immediately blocks Home), inner content div fades in from `opacity: 0` to give the smooth entrance effect. | ✅ Fixed | `82778c0` | Apr 2026 |
| DEF-23 | Settings "Reset Game Data" called `localStorage.clear()`, wiping `hints_remaining` and restoring the default 12 free hints on next launch — players could exploit this to get unlimited free hints. Fix: replaced `localStorage.clear()` with selective key removal (`wf_progress`, `wf_settings`, `wf_daily`, `wf_welcome_seen`, `games_completed_count`, `last_ad_completed_at`). Before clearing, reads and preserves `hints_remaining` (current balance) and `ads_removed` (IAP flag). Restores both after the reset. Toast updated to confirm hints and purchases are preserved. | ✅ Fixed | `dc87dcd` | Apr 2026 |
| DEF-20 | Mystery word from wrong category — e.g. SINGULARITY (space bonus pair) appearing in a Sports game. DEF-17 fixed the *filler overshooting* case, but a second case was missed: when the filler word pool exhausts before landing on a valid category mystery length, the remainder K has no in-category match, and `findMysteryWord` falls through to cross-category words. Root cause: every category had gaps in its word length coverage (e.g. sports had no 11-letter words). Fix: expanded all 14 `wordLists` arrays to cover every length from 4–12 — adding 137 new themed words total (e.g. sports gains PARASAILING, KITESURFING, CHAMPIONSHIP; countries gains AUSTRALIA, MADAGASCAR, SWITZERLAND). With full 4–12 coverage, the filler loop's `validMysteryLengths` stop-check always finds a matching category length, and `findMysteryWord` always returns a themed word. Also improves game variety for regular players. | ✅ Fixed | `cffea23` | Mar 2026 |
| DEF-19 | Touch scroll and pull-to-refresh conflict with word selection on mobile web. React 17+ registers touch listeners as passive by default, so `e.preventDefault()` in synthetic event handlers (`onTouchStart`, `onTouchMove`) is silently ignored — the browser still scrolls/pull-to-refreshes through the game board during word selection. Fix: added a `useEffect` in `GameBoard.jsx` that attaches direct non-passive DOM listeners (`{ passive: false }`) for `touchstart` and `touchmove` on the grid element, ensuring `preventDefault()` is honoured. Also added `overscrollBehavior: 'none'` to the board container to block pull-to-refresh at the CSS level. | ✅ Fixed | `caa3330` | Mar 2026 |
| DEF-24 | Mystery Word mode sporadically fails to generate a mystery word in specific categories at Medium level and above. Root cause: the filler pool is category-restricted (`buildFillerWordPool` draws only from `wordLists[category]`), so at higher levels where many words are removed for the main game, the pool exhausts before the remaining empty cell count (K) drops to a value that matches any word length in the category. With K stuck at e.g. 30, `findMysteryWord` returns null and the mystery phase never activates. The problem worsened at Expert/Master (15×15 grid, 20–25 main words removed) and was masked for Random (filler pool = all categories, ~700+ words). Fix — two-part: (1) expanded every category wordList from 40–65 words to 86–112 words, adding ~487 new themed words total; (2) added a padding fallback in `generateGame()` — after filler exhausts, if K is still not a valid mystery length, random letters are written into empty cells (reading order) until K drops to the largest valid length ≤ K, guaranteeing `findMysteryWord` is always called with a reachable target length. | ✅ Fixed | `ff208e4` | Apr 2026 |
| DEF-27 | Mystery Word mode: some grid cells were "orphaned" — not part of any found word and not highlighted amber as mystery word cells. Root cause: the DEF-24 padding fallback filled cells with random letters to bridge K to a valid mystery length; these random-letter cells were invisible from the game's perspective (not in `bonusLetterPositions`) but visible to the player as unexplained leftover letters. Separately, if the filler loop placed a word that caused K to drop below the smallest valid mystery length (undershoot), `targetK` became 0, all remaining cells were filled with random letters, and the mystery word phase never activated at all. Fix: replaced the random-letter padding fallback with an undo mechanism in the filler loop — before committing any filler word placement, a grid snapshot is taken; if the placement would reduce K below `minValidLength`, the snapshot is restored (cells emptied back) and that filler word is skipped. The loop continues to the next candidate. This guarantees K never undershoots. The random-letter padding fallback is removed entirely; `findMysteryWord`'s existing cross-category fallback handles any residual K not covered by the primary category pool. | ✅ Fixed | `d206cdc` | Apr 2026 |
| DEF-31 | URL params (mode, category, level) silently lost on every game launch after CR-36 switched to HashRouter. Root cause: `window.location.search` is always empty with HashRouter — params live in the hash fragment, not the query string. `Game.jsx` was calling `new URLSearchParams(window.location.search)` which returned an empty object, so all params fell back to defaults. Fix: replaced with `useSearchParams()` hook from react-router-dom, which is router-aware. | ✅ Fixed | `a081fb9` | Apr 2026 |
| DEF-32 | Eye (reveal) button in Word Association mode was incorrectly `disabled` when `hints === 0`, preventing the HintModal (watch ad / buy hints) from opening. Root cause: `disabled` prop applied unconditionally when hint count was zero. Fix: removed `disabled` prop — `handleRevealWord` already handles the zero-hints case by opening HintModal. | ✅ Fixed | `562a3bc` | Apr 2026 |
| DEF-33 | Stats page showed zeros — scores and words found were never being saved. Root cause: stale closure bug in `handleWordFound` (wrapped in `useCallback`). `progress`, `score`, and `hintsRemaining` were captured as `null`/`0` at creation time because `loadProgressData()` is async; by the time the game started, `progress` was still `null`, causing `saveProgress` to return early. Fix: added `progressRef`, `scoreRef`, `hintsRemainingRef` following the existing ref pattern in `Game.jsx`, so `handleWordFound` always reads current values. | ✅ Fixed | `9c9be23` | Apr 2026 |
| DEF-35 | Mystery Word mode: a word could appear in the Words to Find list without being placed in the grid. Root cause: edge case in the filler loop where a word ends up in `placedWords` but its `wordPositions` entry is subsequently deleted (undo/overlap interaction). Result: hint system marks the word as found without highlighting any cells, and the word can never be found by the player. Fix: filter `placedWords` against `wordPositions` before returning from `generateGame` — any word without a grid position is silently dropped from the word list. | ✅ Fixed | `2b5b6d9` | Apr 2026 |
| DEF-36 | All game modes defaulted to Standard / Random / Easy regardless of selection. Root cause: same HashRouter regression as DEF-31 — `window.location.search` is always empty with HashRouter; mode/category/level params were lost. Fix: replaced `new URLSearchParams(window.location.search)` with `useSearchParams()` from react-router-dom in `Game.jsx`. ESLint `no-restricted-syntax` rules added to `eslint.config.js` banning `window.location.search`, `.hash`, and `.assign` in components — build now errors with a message pointing to the correct hook to prevent recurrence. | ✅ Fixed | `a30f2d3` + `b6925e5` | Apr 2026 |
| DEF-37 | Game grid left-justified in portrait on wide screens (tablets, PCs). Root cause: portrait board container has `width: 100%` but `boardSize` is constrained by height (`min(55dvh, 100vw)`), making the board narrower than its container on wide screens. No horizontal centering applied so the board defaulted left. Landscape layout already had `justifyContent: 'center'` — portrait was missing it. Fix: added `display: flex; justifyContent: center; alignItems: flex-start` to the portrait board area container in `Game.jsx`. Narrow phones unaffected — board fills the container on narrow viewports. Confirmed fixed on Android tablet. | ✅ Fixed | `371ddd7` | Apr 2026 |
| DEF-38 | Audio silent on first tap in Android Capacitor build (and potentially iOS). Root cause: `AudioContext` starts in `suspended` state on Android/iOS due to autoplay policy. `unlockAudio()` called `_audioCtx.resume()` without awaiting it, then audio was scheduled immediately — before the context finished resuming. Fix: made `unlockAudio()` async and added `await ctx.resume()` before returning. Updated all call sites (`WordList.jsx`, `Settings.jsx`, `Game.jsx`) to `await unlockAudio()` before scheduling audio playback. Confirmed working on Android tablet. | ✅ Fixed | `371ddd7` | Apr 2026 |
| DEF-39 | Android launcher icon and splash screen showed default Capacitor placeholder (blue X icon) instead of SoundFind branding. Root cause: CR-36 scaffolded the Capacitor Android project but did not generate branded assets. Fix: ran `npx @capacitor/assets generate --android` using `public/icon.png` as source — generated 74 assets across all mipmap densities (ldpi → xxxhdpi) including adaptive icons, round icons, and portrait/landscape splash screens for light and dark modes. Source file copied to `assets/icon.png` as required by `@capacitor/assets`. Confirmed branded icon and splash on Android tablet. | ✅ Fixed | `371ddd7` | Apr 2026 |
| DEF-40 | App displayed in light mode on Android tablet despite dark design intent. Root cause: `Layout.jsx` was toggling the `dark` class based on `prefers-color-scheme` media query — so tablets and PCs in light system preference displayed the app in the unthemed (light) state. Fix: `Layout.jsx` initially force-patched to always add `dark` class (`7ea9316`). Subsequently superseded by CR-38 which replaced the force-dark with a proper user-controlled Default/Light/Dark setting — `Layout.jsx` now reads `theme` from saved settings and follows system preference when set to Default. | ✅ Fixed | `7ea9316` → `94850d6` | Apr 2026 |

---

## How to Raise a Change Request

1. Describe the change — what, why, which files are affected
2. Await approval before any code changes are made
3. Upon completion, record the CR here with commit hash and date

## How to Raise a Defect

1. Describe the defect — steps to reproduce, expected vs actual behaviour, mode/category/level if relevant
2. A fix will be scoped and may be raised as a CR if significant, or applied directly if minor
3. Record the DEF here with fix commit hash and date upon resolution